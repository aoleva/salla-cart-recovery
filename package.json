{
  "name": "salla-cart-recovery",
  "version": "2.1.0",
  "description": "Multi-store Salla abandoned cart recovery with merchant settings and WhatsApp QR linking",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node index.js",
    "check": "node --check index.js && node --check lib/state.js && node --check lib/recovery.js && node --check public/dashboard.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@salla.sa/embedded-sdk": "^0.2.6",
    "axios": "^1.12.2",
    "dotenv": "^16.6.1",
    "express": "^4.21.2",
    "helmet": "^8.1.0",
    "pg": "^8.16.3"
  }
}
